/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['imgur.com', 'i.imgur.com', 'static1.anpoimages.com','cryptosea-nft-marketplace.infura-ipfs.io'],
    },
}

module.exports = nextConfig
